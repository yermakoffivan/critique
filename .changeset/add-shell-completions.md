---
'critique': patch
---

Add shell tab completion. Run `critique completions install`, restart your shell, and Tab completes commands, subcommands, and options. Completions read the live command table, so they never go stale. Remove them with `critique completions uninstall`.
